{
  "name": "diploma-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "devServer": "cd server/ && npm run dev",
    "devClient": "cd client/ && npm run serve",
    "install_modules": "npm i && cd client/ && npm i && cd ../server && npm i && cd ../",
    "launch_app_dev": "npm-run-all --parallel devClient devServer"
  },
  "main": "index.js",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
